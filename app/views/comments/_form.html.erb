<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
    <script src='https://www.google.com/recaptcha/api.js'></script>
  </head>
  <body>

    <div class="col-md-0 comments">
      <h3 class="comments_title"><strong>Comments</strong></h3>
      <%= form_for [@product, Comment.new] do |f| %>
      <%= render 'shared/error_messages', object: f.object %>

      <%= f.label :name, class: "comment_label_title" %>
      <%= f.text_field :name, class: 'form-control comment_name' %>
      <p></p>
      <%= f.label :body, class: "comment_label_title" %>
      <%= f.text_area :body, required: true, class: 'form-control btn-form comment_form' %>

      <%# f.label :rating, class: "comment_label_title" %>
      <!-- <div class="form-group" id="rating-ability-wrapper", style="text-align:center;">
        <label class="control-label" for="rating">
          <span class="field-label-info"></span>
          <input type="hidden" id="selected_rating" name="selected_rating" value="" required="required">
        </label>
        <h2 class="bold rating-header" style="">
          <span class="selected-rating">0</span><small> / 5</small>
        </h2>
        <button type="button" class="btnrating btn btn-default btn-lg" data-attr="1" id="rating-star-1">
          <i class="fa fa-star" aria-hidden="true"></i>
        </button>
        <button type="button" class="btnrating btn btn-default btn-lg" data-attr="2" id="rating-star-2">
          <i class="fa fa-star" aria-hidden="true"></i>
        </button>
        <button type="button" class="btnrating btn btn-default btn-lg" data-attr="3" id="rating-star-3">
          <i class="fa fa-star" aria-hidden="true"></i>
        </button>
        <button type="button" class="btnrating btn btn-default btn-lg" data-attr="4" id="rating-star-4">
          <i class="fa fa-star" aria-hidden="true"></i>
        </button>
        <button type="button" class="btnrating btn btn-default btn-lg" data-attr="5" id="rating-star-5">
          <i class="fa fa-star" aria-hidden="true"></i>
        </button>
      </div> -->

      <p class="title_rating">Rating</p>
      <div class="rating">
        <%= f.collection_radio_buttons(:rating, [[1], [2], [3], [4], [5]], :first, :last) do |b| %>
        <%= b.label(class: 'star') { fa_icon "star" } %>
        <%= b.radio_button class: "radio_but" %>
      <% end %>
      </div>

      <%#= recaptcha_tags class: "recaptcha" %>

      <div class="col-sm-12 text-center comment_button">
        <%= f.submit 'Create comment', remote: true, class: 'btn btn-primary btn-comment' %>
        <hr>
      </div>
      <% end %>
    </div>
  </body>
</html>
