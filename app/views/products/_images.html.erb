<div class="left">
<% if @product.pictures.present? %>
  <% @product.pictures.each do |picture| %>
    <% if @product.pictures.first == picture %>
      <%= link_to image_tag(picture.url(:large)), asset_url(picture.url(:large)), data: { lightbox: "roadtrip" }, class: 'show_main_image' %>
      <br>
    <% else %>
      <%= link_to image_tag(picture.url(:thumb)), asset_url(picture.url(:large)), data: { lightbox: "roadtrip" }, class: "show_other_images" %>
    <% end %>
  <% end %>
<% end %>
</div>
